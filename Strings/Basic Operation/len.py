''' The string len() function returns the length of the string. In this article, we will see how to find the length
of a string using the string len() method.
'''

string="Joha"
print(len(string))

# Here string is the parameter of which we want to find length


# Python len() TypeError
print(len(True))

# TypeError: object of type 'bool' has no len()